# IMPLEMENTATION_NOTES — aquatyping（Type Rogue）

> 仕様書に書かれていなかった判断を、下したその瞬間に記録する実装ノート。
> git にはコミットしない（.gitignore 済み）。フェーズ3レビューの監査入力。

## 案件：きせかえブラッシュアップ（2026-08-08 開始）

- 計画：`~/.claude/plans/lexical-bubbling-lightning.md`（オーナー承認済み）
- アセット仕様：`concept_art/avatar/README.md`

## 判断ログ

### 2026-08-08 実装ノート対象化
- **状況**: きせかえブラッシュアップ案件の開始。複数日・コード実装・仕様書起点のため対象化ルールに該当
- **判断**: リポジトリルートに本ファイルを作成し `.gitignore` に追記
- **理由**: グローバル運用ルール（IMPLEMENTATION_NOTES.md 運用ルール）に従う
- **仕様との関係**: 運用ルールの適用（仕様外事項なし）
- **影響範囲**: .gitignore

### 2026-08-08 フェーズ2-2: クロマキー色の実測値
- **状況**: prepare-avatar.py のパレット定義。README/仕様は背景を純マゼンタ #ff00ff と記載
- **判断**: 実際のクロマキー色は gpt-image 由来で #f406e9 前後（(244,6,233)）。パレットスナップの背景ターゲットを実測値 (244,6,233) にした。純マゼンタから遠いため識別には支障なし
- **理由**: 原画をNEARESTで128量子化した実測。純 #ff00ff だと距離計算がわずかにずれる
- **仕様との関係**: 仕様の詳細化（裁量: パレットスナップの距離計算方式）
- **影響範囲**: scripts/avatar/avatar_common.py の PALETTE
- **要確認**: なし

### 2026-08-08 フェーズ2-2: 素体パレットの実測3値
- **状況**: モス/クリームの shade/base/hi を確定する必要
- **判断**: 原画実測の段階シェードを採用。モス shade(112,148,98)/base(134,170,116)/hi(171,199,148)、クリーム shade(230,209,170)/base(237,222,192)/hi(250,235,206)。README表の body-moss 基本色 #88a97a(136,169,122) と実測 base(134,170,116) は差が不可視のため、スプライトは「原画由来」として実測値を採用（economy.js のスウォッチ色 #88a97a はUI表示用に別管理で不整合なし）
- **理由**: 「基準体（原画由来）」の記述に忠実。原画の色をそのまま量子化するのが自然
- **仕様との関係**: 仕様の詳細化（裁量: 閾値の微調整）
- **影響範囲**: avatar_common.py、body-*.png、recolor のHSL基準
- **要確認**: スウォッチ #88a97a とスプライト実測 base の色差を許容してよいか（河内確認）

### 2026-08-08 フェーズ2-2: 共通モジュール新設
- **状況**: prepare/split/recolor/validate の4本がパレット定義とスナップ処理を共有
- **判断**: scripts/avatar/avatar_common.py を新設しパレット定数・スナップ・センタリング・HSL変換を集約（DoDの4本とは別の内部実装ファイル）
- **理由**: パレット値のドリフト防止。裁量範囲（関数分割・ファイル分割）内
- **仕様との関係**: 仕様外の追加（内部実装。裁量内）
- **影響範囲**: scripts/avatar/ 配下4スクリプトが import
- **要確認**: なし

### 2026-08-08 フェーズ2-2: 素体単体での襟アーク残り
- **状況**: split の body-moss で、原画のポンチョ襟（頭と胴の境界）の黒輪郭がクリーム→モス写像後も体側に残る。仕様は「頭と襟の境界の輪郭が素体側と二重になるのは許容（同色のため見えない）」と記載
- **判断**: そのまま許容。服を着せた状態では outfit-cloth が覆うため不可視。服なし状態（プレビューの上段）では胸元に薄い輪郭アークが見えるが、2頭身チビキャラの体の輪郭・胸の区切りとして自然に読めるため救済処理は入れない
- **理由**: 仕様が二重輪郭を明示的に許容。無理に消すと素体シルエットが崩れるリスク
- **仕様との関係**: 仕様の想定内（許容事項の確認）
- **影響範囲**: public/avatar/body-*.png（服なし表示時の見え方）
- **要確認**: 服なし単体表示を実運用でどこまで見せるか（見せるなら襟アークの評価を河内に確認）

### 2026-08-08 フェーズ2-2: body-night のデータ未接続（スコープ順守）
- **状況**: body-night は economy.js の ITEMS に未登録（既存は moss/sky/peach の3色のみ）。本フェーズは src/ 変更がスコープ外
- **判断**: アセット body-night.png のみ生成し、economy.js への item 追加・ワイヤリングは行わない（後続フェーズ）
- **理由**: 申し送り「src/ 配下の変更は本フェーズのスコープ外」に従う
- **仕様との関係**: スコープ順守（スコープOut事項に手を出さない）
- **影響範囲**: なし（アセットのみ先行整備）
- **要確認**: body-night を economy.js に追加する後続フェーズの有無

### 2026-08-08 フェーズ2-2: プレビュー合成の実装
- **状況**: DoD#3 の preview-base.png 生成。README は将来の全アイテムQA用に compose-avatar-preview.mjs を挙げるが本フェーズ対象は preview-base.png のみ
- **判断**: 恒久スクリプト化せず scratchpad の使い捨て python で 4色×ふく有無×(白/暗2段) の8組グリッドを合成。.mjs の全アイテムQAツールは後続フェーズで整備
- **理由**: DoDは preview-base.png ファイルの成果物のみ要求。スコープ最小化
- **仕様との関係**: 仕様の詳細化（プレビューレイアウトは裁量範囲）
- **影響範囲**: tmp/imagegen/avatar/preview-base.png
- **要確認**: 全アイテムQA用 compose-avatar-preview.mjs を後続で作るか

### 2026-08-08 差し戻し修正: 縁はみ出し侵食でエスカレーション停止
- **状況**: 差し戻し修正指示（境界の非ink色ドット除去）に従い avatar_common.py に strip_color_fringe（透明隣接の非ink可視画素を透明化、反復上限4、削除3%超で停止）を実装し実行
- **判断**: 反復上限4に達してもなお非ink境界が残存したため、修正指示のエスカレーション条件どおり**作業を停止**。回避策（削除でなく輪郭色への塗り替え等）は自力実施しない
- **理由**: 診断の結果、単純削除では解決しない構造問題を確認。原画の黒輪郭に「斜め接続のみの階段部」が複数あり（頭の右上 (34,89) 付近、左肩 (52,37) 付近、右腕付け根 (72,89) 付近など）、そこでは輪郭の対角ギャップを通じて内側の色画素が透明と4近傍接触する。削除すると次の色画素が新たに露出し、削除数が発散（各パスの削除数 14→5→8→11→17→22 と増加、6パス累計77px）。キャラ内部を食い進む「侵食チャネル」になっている
- **仕様との関係**: 修正指示のエスカレーション条件に該当（仕様どおりの停止）
- **影響範囲**: アセット未再生成（public/concept_art とも差し戻し前の版のまま）。validate-avatar.py には境界inkチェックを追加済みのため、現行アセットに対し validate は失敗する状態
- **要確認**: 対処方針の設計判断。候補: (a) 境界の非ink画素を削除でなく**輪郭色に塗り替える**（シルエット保存・発散しない） (b) 斜めギャップ検出箇所のみ輪郭画素を追加して埋める (c) 真のはみ出し（1パス目の純粋な外側ドット）だけ削除し、以降は塗り替えに切り替えるハイブリッド

### 2026-08-08 差し戻し修正: 塗り替え方式の裁定と実装（設計側の裁定）
- **状況**: 上記エスカレーションに対し設計側が **(a) 塗り替え方式** を裁定（輪郭の閉性保証・穴を作らない・1〜2パス収束が理由。輪郭1px太りは階段輪郭に吸収される見込み、最終判定は目視レビュー）
- **判断**: strip_color_fringe を「境界の非ink可視画素→輪郭色に塗り替え」に改修。裁定の裁量前処理（透明4近傍3つ以上の完全浮遊ドットは削除）も採用——採用しないと頭上の浮きドット (16,71) 等が黒い孤立点として外に残るため
- **理由**: 裁定どおり。前処理の採用可否は目視判断せよとの指定→再生成後の目視で黒残り・不自然な欠けなしを確認し採用確定
- **仕様との関係**: 設計裁定の忠実な実装（裁量は前処理の採用判断のみ）
- **影響範囲**: avatar_common.py / prepare-avatar.py / split-avatar-layers.py。実行結果: シルエット=塗り替え10px・削除4px（1パス収束）、outfit=塗り替え13px・削除0px（1パス収束）。全アセット再生成・validate（境界inkチェック込み）全パス
- **要確認**: 塗り替えによる輪郭の微小な段差（頭右上ほか）の見た目最終判定はフェーズ3目視レビュー

### 2026-08-08 フェーズ4: 服の点数の仕様不一致（4着 vs 5点）
- **状況**: 申し送りDoD#1の見出しは「服4着」だが、直後に rain/sun/stripe/scale/deep の**5点**を列挙。READMEアイテムカタログの outfit スロット新規も5点。申し送り冒頭・README も「計21個／新規12・確定14アセット」＝ 5服+9頭手 で辻褄が合う
- **判断**: 「4着」を誤記とみなし、列挙された5点すべてを制作（14アセット total と整合）
- **理由**: 具体列挙＋総数14の二重の根拠が「5」を支持。数値の見出し1箇所より列挙とカタログを優先
- **仕様との関係**: 仕様内（曖昧さの解消。列挙・総数に忠実）
- **影響範囲**: outfit-*.png 5点
- **要確認**: なし（14アセットで完結）

### 2026-08-08 フェーズ4: 恒久スクリプト3本の新設
- **状況**: 既存パイプライン（avatar_common / prepare / split / recolor / validate）は素体専用。服の量産・生成アイテムの配置・全アイテムプレビューに専用処理が要る
- **判断**: scripts/avatar/ に make-outfits.py（服のマスク内加工）/ make-item.py（頭手の透過→量子化→アンカー配置）/ compose-avatar-preview.py（DoD#5の恒久プレビュー）を新設。いずれも avatar_common を import して量子化・パレット・LOGICAL/OUTPUT・HSL 導出を流用（独自の別処理は新設しない方針を順守）
- **理由**: 裁量範囲（ファイル分割・レイアウト）。再現性のため使い捨てにせず恒久化
- **仕様との関係**: 仕様外の追加（内部実装。裁量内）
- **影響範囲**: scripts/avatar/ に3ファイル追加
- **要確認**: なし

### 2026-08-08 フェーズ4: 生成アイテムの量子化はPALETTE非依存で別系統
- **状況**: avatar_common.PALETTE は bg/outline/moss/cream 専用。帽子・持ち物は各自の色（緑/紺/黄/桃/水色…）を持ち、既存パレットにスナップできない
- **判断**: make-item.py では「純色クロマキーを距離で透過→アイテム固有色はそのまま保持→NEARESTでターゲット論理サイズへ縮小→アンカー配置」の別系統量子化にした。境界ink保証は strip_color_fringe の思想（塗り替え方式・完全浮遊ドット削除）を汎用RGBAグリッド版として再実装（INK判定は max(channel)<60）。量子化そのものは素体と同じ128論理→256の2倍系を厳守
- **理由**: 素体パレットに縛ると多色アイテムが表現不能。境界ink処理の「削除でなく塗り替え」裁定は全アイテム共通の品質基準なので流用
- **仕様との関係**: 仕様の詳細化（「量子化は素体と同じ128論理系」「境界処理を適用」に忠実。パレットは素体固有のため別扱い）
- **影響範囲**: make-item.py（load_logical / crop_and_scale / guarantee_border_ink）
- **要確認**: なし（validate 境界inkチェック全パス）

### 2026-08-08 フェーズ4: 帽子は頭に「かぶせる」配置（上に浮かせない）
- **状況**: キャンバス上余白は約12%（論理16px）しかなく、頭頂(y16)より上に帽子全体を置くと画面外にはみ出す（初回 head-leaf が上端クリップで validate 失敗）
- **判断**: head系は「item下端を bottom_y に置き、クラウンは上へ伸ばす」方式にし、bottom_y を頭の中〜上部（例 leaf=33, shell=34, star=44, diver=eye_row中心, lantern=24）に設定。帽子は頭に重なって座る
- **理由**: 12%余白の設計意図＝帽子は素体に被さる前提。塔のように上に伸ばす前提ではない
- **仕様との関係**: 仕様の詳細化（「頭頂に2〜4px重ねて中央」を、被せ方向の重なりとして実装）。READMEの基準位置節に実測値・配置規則を記入（DoD#4）
- **影響範囲**: make-item.py の ITEMS 配置、README「基準位置」
- **要確認**: 各帽子のかぶり深さ（bottom_y）の見た目はフェーズ3目視で最終判定

### 2026-08-08 フェーズ5: 浮遊アニメを .avatar でなく .avatar-layer に掛けた
- **状況**: 申し送りDoD#2「コンテナに CSS 浮遊アニメ（translateY）」。既存 avatar.css のモバイル用 `@media (max-width:560px) .avatar { transform: scale(.8) }` が残っており、同じ要素に float アニメ（transform 使用）を掛けると 0%/100% キーフレームで transform が none にリセットされ、モバイルの縮小が毎周期解除される
- **判断**: float アニメを各 `.avatar-layer`（img）側に付与。全レイヤーが同一タイムラインで同期し、まとまって上下する。コンテナ `.avatar` の transform（モバイル縮小）と食い合わない
- **理由**: 「コンテナに掛ける」文言より、既存モバイル transform を壊さない実装意図を優先。見た目は container 掛けと等価
- **仕様との関係**: 仕様の詳細化（DoD の意図＝相棒が上下に浮く、を満たす）
- **影響範囲**: src/styles/components/avatar.css（animation を .avatar-layer に、reduced-motion も同要素に）
- **要確認**: なし（reduced-motion は media query＋既存 `.reduce-motion *` の二重で停止）

### 2026-08-08 フェーズ5: きせかえプレビューの表示方式（スウォッチ/サムネイル/プレースホルダ）
- **状況**: DoD#3。asset サムネイル表示が基本、からだの色は「色スウォッチ表示でもよい（裁量）」
- **判断**: bodyColor かつ color あり→色スウォッチ（従来の丸）、それ以外で asset あり→64px 透過サムネイル（object-fit:contain・pixelated・下地 rgba(255,255,255,.06)）、asset のない〜none→プレースホルダ「●」。スロット名は SLOT_LABELS で4スロット化（もちもの追加）
- **理由**: からだは色選択なのでスウォッチが直感的。装飾アイテムは実物のドット絵を見せる方が選びやすい。裁量範囲内
- **仕様との関係**: 仕様の詳細化（裁量: プレビュー表示・CSS クラス設計）
- **影響範囲**: GameShell.jsx WardrobeScreen、settings.css（.item-preview[data-thumb] / .item-thumb 追加）
- **要確認**: なし

### 2026-08-08 フェーズ5: 旧セーブでの hand-none は「所持」補完せず equipped のみ補完
- **状況**: 申し送りDoD#1「equipped.hand 未定義なら hand-none 補完。ownedItemIds は追加式のため変更不要」。旧セーブは equipped.hand を hand-none に補うが ownedItemIds に hand-none を含まない
- **判断**: 申し送りどおり ownedItemIds は補完しない（loadSave の merged で `equipped: {...defaults.equipped, ...migrated.equipped}` のみ追加）。旧セーブで別のもちものに変えた後 hand-none へ戻す操作は、価格0の「みつける」（purchase 0コイン）で自然に再取得され破綻しない
- **理由**: 申し送りの明示指定に忠実。実挙動上の破綻がないことを確認（equip は所持必須だが、未所持の無料アイテムは PURCHASE_OR_EQUIP が 0コイン購入で通す既存経路がある）
- **仕様との関係**: 仕様内（明示指定の順守）
- **影響範囲**: src/domain/save.js（equipped マージのみ）
- **要確認**: 旧セーブでもちもの装備中、hand-none 枠が「0コインで みつける」表示になる点（未所持の無料アイテム全般と同じ挙動）を許容してよいか

### 2026-08-08 フェーズ4: 2アイテムを再生成（3回上限内）
- **状況**: head-lantern 初版は茎が長すぎ玉が9px・可視372px<400でvalidate失敗。hand-net 初版は網目が透けて縮小時に黒ノイズ化（判読困難）
- **判断**: head-lantern＝「玉を主役・茎ごく短く」で再生成（1回）。hand-net＝「網は透けさせず塗りつぶし＋格子線数本」で再生成（1回）。いずれも裁量のプロンプト調整・サイズ調整（lantern h22, net solid）で解決
- **理由**: 申し送り§4裁量（プロンプト再生成3回/アイテム）・§3のスキップ判断に至る前に1回で改善
- **仕様との関係**: 仕様内（裁量の再生成）
- **影響範囲**: head-lantern-source.png / hand-net-source.png（tmp）と両出力png
- **要確認**: なし
